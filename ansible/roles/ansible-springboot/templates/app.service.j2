[Unit]
Description={{ springboot_application_name }}
After=syslog.target

[Service]
User={{ springboot_user }}
ExecStart=/usr/bin/java -jar {{ springboot_deploy_folder }}/{{ springboot_application_name }}.jar

[Install]
WantedBy=multi-user.target